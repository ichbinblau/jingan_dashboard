<%= semantic_bootstrap_nested_form_for @kf_sickness , :url => url_for( :page=>params[:page], :action => params[:action]=="edit" ? "update" : "create") do |f| %>
  <% if @kf_sickness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kf_sickness.errors.count, "error") %> prohibited this kf_sickness from being saved:</h2>

      <ul>
      <% @kf_sickness.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.inputs do %>
    <%= f.input :kf_sort_id %>
    <%= f.input :name %>
    <%= f.input :description %>
  <% end %>
  <%= f.actions do %>
    <%= f.action :submit ,:label => params[:action]=="edit" ? "修改" : "添加" %>
  <% end %>
<% end %>

