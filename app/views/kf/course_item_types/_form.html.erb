<%= semantic_bootstrap_nested_form_for @kf_course_item_type , :url => url_for( :page=>params[:page], :action => params[:action]=="edit" ? "update" : "create") do |f| %>
  <% if @kf_course_item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kf_course_item_type.errors.count, "error") %> prohibited this kf_course_item_type from being saved:</h2>

      <ul>
      <% @kf_course_item_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.inputs do %>
    <%= f.input :order_level %>
    <%= f.input :title %>
    <%= f.input :image_cover %>
    <%= f.input :description %>
  <% end %>
  <%= f.actions do %>
    <%= f.action :submit ,:label => params[:action]=="edit" ? "修改" : "添加" %>
  <% end %>
<% end %>

